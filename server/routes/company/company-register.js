const Router = require('koa-router');
const router = new Router();
const service = require('../../services/company/company-register');

router.get('/' , async ctx => {
    
});


module.exports = router;